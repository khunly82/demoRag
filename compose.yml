services:
  ollama:
    container_name: ollama
    image: ollama/ollama:latest
    ports:
      - 11434:11434
    volumes:
      - ./model_files:/model_files
    entrypoint: ['/bin/sh', '/model_files/ollama.sh']